import * as user from './user';
import * as city from './city';
import * as province from './province';
import * as demand from './demand';

export interface InitData {
    user: user.UserInfo,
    demand: demand.DemandInfo,
    city: city.CityInfo,
    selected_city: city.CityInfo,
    province: province.ProvinceInfo,
    selected_province: province.ProvinceInfo,
    demand_config: demand.demandConfig,
    currIsSelfDemand?: boolean,
    current_demand: demand.DemandInfo,
    project_list: demand.DemandInfo[],
    supplier_vas:user.supplierVas[],
    unread_num: {
        group_unread: string,
        private_unread: string
    },
    joined_list:[any],
    currUserDemandList:[any],
    friend_list:[any]
}